const { crc16 } = require("crc");

const { logMgr } = require('./logger');

/*
unsigned char data1[] = {0x6c, 0xd9, 0x02, 0x40, 0x40, 0x40, 0x50, 0x03};
unsigned char data2[] = {0x6c, 0xd9, 0x02, 0x40, 0xc8, 0x40, 0x50, 0x03};
*/

let b1 = Buffer.from([0x6c, 0xd9, 0x02, 0x40, 0x40, 0x40, 0x50, 0x03]);
let b2 = Buffer.from( [0x6c, 0xd9, 0x02, 0x40, 0xc8, 0x40, 0x50, 0x03]);

logMgr.info(`Testing CRC on b1 = ${crc16(b1).toString(16)}, should be 0x8826 [0x26, 0x88]} `);
logMgr.info(`Testing CRC on b2 = ${crc16(b2).toString(16)}, should be 0x280d [0x0d, 0x28]} `);
